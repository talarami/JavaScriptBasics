// ternary operator - operator trójelementowy

let speed = 150

if (speed > 70) {
    console.log("speed > 70")
} else {
    console.log("speed < 70")
}

let result = speed > 70 ? "speed > 70" : "speed < 70" // znak zapytania ternary, zastępuje to co wyżej
console.log(result)