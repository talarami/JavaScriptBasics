

    // Stwórz tablicę z nazwami prymitywów: string, number itd. Wyświetl tablicę w konsoli.
    // Pokaż pierwszy, trzeci i ostatni element. Zmień wartość drugiego elementu na tą samą
    // wartość ale z wykrzyknikiem na końcu łańcucha znaków. Pokaż ponownie tablicę w konsoli.

    let words = ["string", "number", "object", "null", "bignit", "boolean"]

    
    wordsLen = words.length

    console.log(words.length)

    console.log(words[0])
    console.log(words[3])
    console.log(words[words.length - 1])

    words[2] = "object!"

    console.log(words)







